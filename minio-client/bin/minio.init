echo "init bucket started"
echo $MINIO_HOST
/usr/bin/mc config host add minio http://${MINIO_HOST}:9000 "${MINIO_ACCESS_KEY}" "${MINIO_SECRET_KEY}" --api S3v4
/usr/bin/mc mb -p minio/object-storage
/usr/bin/mc policy set public minio/object-storage/images
echo "init bucket completed"